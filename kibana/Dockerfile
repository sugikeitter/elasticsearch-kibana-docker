#FROM kibana:6.3.1
FROM docker.elastic.co/kibana/kibana:6.3.1

# x-pack のインストールは不要
#```
#Kibana now contains X-Pack by default, there is no longer any need to install it as it is already present.
#ERROR: Service 'kibana' failed to build: The command '/bin/sh -c kibana-plugin install x-pack' returned a non-zero code: 1
#```
#RUN kibana-plugin install x-pack
